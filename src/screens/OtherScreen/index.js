import { connect } from 'react-redux';
import Others from './view';

const mapStateToProps = ({ auth }) => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(Others);